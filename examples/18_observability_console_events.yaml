name: observability_console_events
version: 1
stages:
- stage: obs
  jobs:
  - job: console
    steps:
    - step: start
      type: system.echo
      with: { message: "console event demo" }
    - step: done
      type: system.echo
      depends_on:
      - start
      with: { message: "done" }
