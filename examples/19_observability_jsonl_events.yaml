name: observability_jsonl_events
version: 1
stages:
- stage: obs
  jobs:
  - job: jsonl
    steps:
    - step: one
      type: system.echo
      with: { message: "one" }
    - step: two
      type: system.echo
      depends_on:
      - one
      with: { message: "two" }
