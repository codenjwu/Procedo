name: timeout_failure
version: 1
stages:
- stage: timeout
  jobs:
  - job: timeout_case
    steps:
    - step: slow_step
      type: demo.sleep
      timeout_ms: 100
      with:
        milliseconds: 5000
